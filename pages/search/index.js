import SearchPage from "../../components/SearchPage";
import Layout from "@/layout/Layout";

const Search = () => {
  return (
    <Layout>
      <SearchPage />
    </Layout>
  );
};

export default Search;
